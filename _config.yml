# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Your awesome title
email: your-email@example.com
description: >- # this means to ignore newlines until "baseurl:"
  Write an awesome description for your new site here. You can edit this
  line in _config.yml. It will appear in your document head meta (for
  Google search results) and in your feed.xml site description.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: jekyllrb
github_username:  jekyll

# Build settings
theme: minima
plugins:
  - jekyll-feed

# ============================================
# CONTENT COLLECTIONS
# ============================================
# `notes` is the publishing collection. Drop a Markdown file in
# _notes/ and it becomes a real page at /notes/<filename-slug>/.
#
# A collection is used instead of _posts because _posts is a single
# global stream with a single global permalink. Reserving it for Notes
# would leave Projects unable to use the same drop-a-file workflow
# later. Collections scale: _projects can be added the same way.
#
# RESERVED SLUGS: do not name a note thoughts.md, essays.md, or
# inspiration.md — those would generate /notes/thoughts/ etc. and
# shadow the router's scroll-anchor URLs.
collections:
  notes:
    output: true
    permalink: /notes/:name/

# Every document in _notes gets these automatically, so individual
# post files only need title / date / category / image.
#
#   layout: note   -> the shared post presentation
#   router: "off"  -> tells main.js this is a real page, not a router
#                     shell, so it must not overwrite the content
#   section: notes -> which nav parent to highlight
#
# Any of these can still be overridden per-file. A future post that
# wants to be its own little webpage just sets its own `layout:` in
# front matter; nothing else here has to change.
defaults:
  - scope:
      path: ""
      type: "notes"
    values:
      layout: "note"
      router: "off"
      section: "notes"

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
